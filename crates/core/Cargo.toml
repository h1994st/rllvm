[package]
name = "rllvm-core"
version = "0.6.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Core library for rllvm: argument classification, bitcode capture and catalogs"
categories = ["development-tools", "development-tools::build-utils"]
keywords = ["llvm", "clang", "bitcode", "IR"]
readme = "README.md"
exclude = ["tests/*"]

# A library with no binaries. dist would otherwise announce it as an app and
# publish an empty archive and installer alongside the two that ship binaries;
# it reaches users through crates.io, from the release's publish job.
[package.metadata.dist]
dist = false

[dependencies]
clap.workspace = true
object.workspace = true
owo-colors.workspace = true
regex = "~1.11.3"
serde.workspace = true
serde_json.workspace = true
tempfile.workspace = true
toml.workspace = true
tracing.workspace = true
which.workspace = true
thiserror = "~2.0"
sha2 = "0.10"
md-5 = "0.10"
sha1 = "0.10"

[target.'cfg(target_vendor = "apple")'.dependencies]
glob.workspace = true

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "rllvm_benchmarks"
harness = false
